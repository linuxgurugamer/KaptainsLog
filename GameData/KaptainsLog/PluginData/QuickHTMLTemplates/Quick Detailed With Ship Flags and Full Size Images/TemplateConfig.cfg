//
// This is an example file you can copy and customize
//
TemplateConfig
{
	isHtml = true			// true if this is an HTML file
	isQuickExport = true	// true for quick export templates
	entriesPerPage = 5		// Specifies how many entries are put on each page.  If not there, then no paging is done.
	fileSuffix = html		// Optional file suffix
	header = header.tmpl	//
	detail = detail.tmpl
	footer = footer.tmpl
}
